# homebrew-glint

Homebrew tap for [Glint](https://github.com/chenbstack/glint) — a polished macOS terminal for AI agents.

## 安装

```bash
brew tap chenbstack/glint
brew install --cask glint
```

## 升级 / 卸载

```bash
brew update && brew upgrade --cask glint
brew uninstall --cask glint
```
